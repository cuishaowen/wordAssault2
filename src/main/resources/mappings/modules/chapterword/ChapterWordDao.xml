<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.thinkgem.jeesite.modules.chapterword.dao.ChapterWordDao">
    
	<sql id="chapterWordColumns">
		a.id_ AS "id",
		a.course_id_ AS "courseId",
		a.chapter_id_ AS "chapterId",
		a.word_ids_ AS "wordIds",
		a.create_date_ AS "createDate",
		a.update_date_ AS "updateDate",
		a.remarks_ AS "remarks",
		a.del_flag_ AS "delFlag",
		a.blank_one_ AS "blankOne",
		a.blank_two_ AS "blankTwo"
	</sql>
	
	<sql id="chapterWordJoins">
	</sql>
    
	<select id="get" resultType="ChapterWord">
		SELECT 
			<include refid="chapterWordColumns"/>
		FROM chapter_word a
		<include refid="chapterWordJoins"/>
		WHERE a.ID_ = #{id}
	</select>

	<select id="getByChapterId" resultType="ChapterWord">
		SELECT
			<include refid="chapterWordColumns"/>
		FROM chapter_word a
		WHERE a.CHAPTER_ID_ = #{chapterId}
	</select>
	
	<select id="findList" resultType="ChapterWord">
		SELECT 
			<include refid="chapterWordColumns"/>
		FROM chapter_word a
		<include refid="chapterWordJoins"/>
		<where>
			
			<if test="id != null and id != ''">
				AND a.id_ = #{id}
			</if>
			<if test="courseId != null and courseId != ''">
				and a.course_id_ = #{courseId}
			</if>
			<if test="chapterId != null and chapterId != ''">
				and a.chapter_id_ = #{chapterId}
			</if>
		</where>
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
			</otherwise>
		</choose>
	</select>
	
	<select id="findAllList" resultType="ChapterWord">
		SELECT 
			<include refid="chapterWordColumns"/>
		FROM chapter_word a
		<include refid="chapterWordJoins"/>
		<where>
			
		</where>		
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
			</otherwise>
		</choose>
	</select>
	
	<insert id="insert">
		INSERT INTO chapter_word(
			id_,
		    COURSE_ID_,
			chapter_id_,
			word_ids_,
			create_date_,
			update_date_,
			remarks_,
			del_flag_,
			blank_one_,
			blank_two_
		) VALUES (
			#{id},
		    #{courseId},
			#{chapterId},
			#{wordIds},
			#{createDate},
			#{updateDate},
			#{remarks},
			#{delFlag},
			#{blankOne},
			#{blankTwo}
		)
	</insert>
	
	<update id="update">
		UPDATE chapter_word SET 	
			id_ = #{id},
		    COURSE_ID_ = #{courseId},
			chapter_id_ = #{chapterId},
			word_ids_ = #{wordIds},
			create_date_ = #{createDate},
			update_date_ = #{updateDate},
			remarks_ = #{remarks},
			del_flag_ = #{delFlag},
			blank_one_ = #{blankOne},
			blank_two_ = #{blankTwo}
		WHERE ID_ = #{id}
	</update>
	
	<update id="delete">
		DELETE FROM chapter_word
		WHERE ID_ = #{id}
	</update>
	
</mapper>